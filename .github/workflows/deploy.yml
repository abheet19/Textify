name: Legacy GCP deployment retired
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  migration-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Textify uses the existing textify-abheet19 Fly app. Use the verified Fly deployment workflow; this legacy GCP job intentionally makes no cloud changes.'
